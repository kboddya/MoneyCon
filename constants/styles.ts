import { TextStyle, ViewStyle } from "react-native";

export const SettingsControlStyles = {
    selectionControlStyle: {
        width: "100%",
        marginTop: 5,
        borderRadius: 20,
        alignSelf: "center",
    } as ViewStyle,
}
